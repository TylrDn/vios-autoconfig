# HMC connection (SSH key authentication only)
HMC_HOST=hmc.example.local
HMC_USER=hscroot
HMC_SSH_KEY=/home/you/.ssh/id_ed25519

# Default behavior
# DRY_RUN=1    # uncomment for dry-run by default
# APPLY=1      # uncomment ONLY when you want scripts to modify systems

# NOTE: Copy to .env and chmod 600 .env

